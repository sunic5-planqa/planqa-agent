# Cheap-first per memory: planqa-model-selection-policy — try gemini/ollama before Claude.
PLANQA_LLM_BACKEND=gemini
PLANQA_LLM_MODEL=

# Single key:
GEMINI_API_KEY=
# Or multiple keys/projects round-robined to multiply the free-tier daily quota
# (used instead of GEMINI_API_KEY when set):
# GEMINI_API_KEYS=key-one,key-two,key-three

# Only used when PLANQA_LLM_BACKEND=ollama
OLLAMA_HOST=http://localhost:11434

# Only used when PLANQA_LLM_BACKEND=anthropic (direct Claude API, not the mindlogic gateway)
ANTHROPIC_API_KEY=
